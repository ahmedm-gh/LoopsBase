import 'package:flutter/material.dart';

class UsefulPluginsDetailsScreen extends StatelessWidget {
  const UsefulPluginsDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text(
          "Useful Plugins Details\nComing Soon...",
          textAlign: .center,
        ),
      ),
    );
  }
}
