import 'package:flutter/material.dart';

class UsefulPluginsScreen extends StatelessWidget {
  const UsefulPluginsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("Useful Plugins\nComing Soon...", textAlign: .center),
      ),
    );
  }
}
